from models.models import (
    Admin, Group, Student, Subject, 
    Exam, Question, PassedExams, ExamDetails
)
from models.schemas import (
    groupSchema, studentSchema, subjectSchema, 
    examSchema, questionSchema, examStatusSchema, resultSchema
)